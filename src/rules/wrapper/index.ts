/**
 * `wrapper/*` rules — cross-file checks on declared native-wrapper
 * components. See `src/rules/wrapper/drift.ts` for the motivating
 * asymmetry these rules close.
 */

export { rule as drift } from "./drift.ts";
