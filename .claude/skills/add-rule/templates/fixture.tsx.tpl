// {{scenario}}
// Minimal reproducer — this file should trigger exactly one violation of
// rule {{domain}}/{{slug}} when scanned.

export function Example() {
  return <div>replace with the smallest possible reproducer</div>;
}
